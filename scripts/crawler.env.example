# Threadstead Crawler Configuration
# Copy this to /path/to/your/threadstead/.env.crawler and configure

# Server URL where your Threadstead instance is running
CRAWLER_SERVER_URL=http://localhost:3000

# Authentication (choose one method)
# Option 1: API Key (recommended for production)
CRAWLER_API_KEY=your-api-key-here

# Option 2: Session Cookie (if using session-based auth)
# CRAWLER_SESSION_COOKIE=session=your-session-cookie-here

# Optional: Override default settings
# CRAWLER_PEAK_BATCH_SIZE=15
# CRAWLER_PEAK_CONCURRENCY=4
# CRAWLER_OFFPEAK_BATCH_SIZE=8
# CRAWLER_OFFPEAK_CONCURRENCY=2